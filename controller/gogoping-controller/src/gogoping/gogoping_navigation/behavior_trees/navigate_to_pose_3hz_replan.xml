<!--
  navigate_to_pose_3hz_replan.xml
  base: nav2 jazzy navigate_to_pose_w_replanning_and_recovery.xml
  변경: RateController hz="1.0" → hz="3.0" — planner 호출 주기를 3배로.

  목적: 0.15 m/s 주행 시 1Hz replan 의 drift (15cm) 가 좁은공간에서 좌우 흔들림
        유발 → 3Hz 로 올려 drift ~5cm 로 축소. CPU 부담은 50% 가량 ↑.

  경로 평활: ComputePathToPose 직후 SmoothPath(SimpleSmoother, smoother_server)로
        planner 출력 path 를 평활해 각진 경로/좌우 흔들림 완화. best-effort —
        평활 실패 시 Fallback→AlwaysSuccess 로 원본 path 그대로 주행(주행 중단 없음).
        ※ 이 BT 를 쓰는 launch 는 smoother_server 노드가 떠 있어야 함
          (navigation_real.launch.xml / sim_with_nav2.launch.xml 에 포함).
-->
<root BTCPP_format="4" main_tree_to_execute="MainTree">
  <BehaviorTree ID="MainTree">
    <RecoveryNode number_of_retries="6" name="NavigateRecovery">
      <PipelineSequence name="NavigateWithReplanning">
        <ControllerSelector selected_controller="{selected_controller}" default_controller="FollowPath" topic_name="controller_selector"/>
        <PlannerSelector selected_planner="{selected_planner}" default_planner="GridBased" topic_name="planner_selector"/>
        <!-- goal_checker 명시 — nav2_params 에 goal_checker 가 2개(general/follow_person)면
             FollowPath 가 goal_checker_id 빈 값일 때 "Failed to find goal checker name:" 로
             실패해 로봇이 안 움직임. ControllerSelector 와 동일 패턴으로 default 지정.
             FOLLOW 가 follow_person_goal_checker 를 쓰려면 goal_checker_selector 토픽으로 publish. -->
        <GoalCheckerSelector selected_goal_checker="{selected_goal_checker}" default_goal_checker="general_goal_checker" topic_name="goal_checker_selector"/>
        <RateController hz="3.0">
          <RecoveryNode number_of_retries="1" name="ComputePathToPose">
            <Sequence>
              <ComputePathToPose goal="{goal}" path="{path}" planner_id="{selected_planner}" error_code_id="{compute_path_error_code}"/>
              <!-- SmoothPath best-effort — 평활 실패해도 원본 path 로 진행(주행 유지). -->
              <Fallback name="SmoothPathBestEffort">
                <SmoothPath unsmoothed_path="{path}" smoothed_path="{path}" smoother_id="simple_smoother"/>
                <AlwaysSuccess/>
              </Fallback>
            </Sequence>
            <Sequence>
              <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
              <ClearEntireCostmap name="ClearGlobalCostmap-Context" service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
          </RecoveryNode>
        </RateController>
        <RecoveryNode number_of_retries="1" name="FollowPath">
          <FollowPath path="{path}" controller_id="{selected_controller}" goal_checker_id="{selected_goal_checker}" error_code_id="{follow_path_error_code}"/>
          <Sequence>
            <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
            <ClearEntireCostmap name="ClearLocalCostmap-Context" service_name="local_costmap/clear_entirely_local_costmap"/>
          </Sequence>
        </RecoveryNode>
      </PipelineSequence>
      <Sequence>
        <Fallback>
          <WouldAControllerRecoveryHelp error_code="{follow_path_error_code}"/>
          <WouldAPlannerRecoveryHelp error_code="{compute_path_error_code}"/>
        </Fallback>
        <ReactiveFallback name="RecoveryFallback">
          <GoalUpdated/>
          <RoundRobin name="RecoveryActions">
            <Sequence name="ClearingActions">
              <ClearEntireCostmap name="ClearLocalCostmap-Subtree" service_name="local_costmap/clear_entirely_local_costmap"/>
              <ClearEntireCostmap name="ClearGlobalCostmap-Subtree" service_name="global_costmap/clear_entirely_global_costmap"/>
            </Sequence>
            <Spin spin_dist="1.57" error_code_id="{spin_error_code}"/>
            <Wait wait_duration="5.0"/>
            <BackUp backup_dist="0.30" backup_speed="0.15" error_code_id="{backup_code_id}"/>
          </RoundRobin>
        </ReactiveFallback>
      </Sequence>
    </RecoveryNode>
  </BehaviorTree>
</root>
