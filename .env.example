# Atlassian Confluence Cloud 인증 정보
# 1. https://id.atlassian.com/manage-profile/security/api-tokens 에서 토큰 생성
# 2. 이 파일을 .env 로 복사 후 값 채우기 (프로젝트 루트에 둔다)
#    cp .env.example .env
# 3. .env 는 .gitignore 로 제외됨

# Atlassian (Confluence sync)
ATLASSIAN_EMAIL=your-email@example.com
ATLASSIAN_API_TOKEN=your-api-token-here

# Database. 테스트는 같은 인스턴스에 DB 이름 + `_test` 를 붙여 자동 생성/삭제한다.
DATABASE_URL=postgresql+asyncpg://pingder:pingder@localhost:5432/pingdergarten

# fastapi-users
SECRET=dev-secret-change-me
COOKIE_MAX_AGE=604800

# robot 디바이스 (등하원 얼굴 인식 endpoint 보호)
ROBOT_DEVICE_TOKEN=dev-robot-token-change-me
FACE_MATCH_THRESHOLD=0.45

# AI Hub (Ollama LLM) / Edge TTS 튜닝 노브는 server/ai/config.py 의 Settings 에 모두
# 동작 가능한 기본값으로 박혀 있다. dev/CI 가동에 별도 env 가 필요 없으며, 모델 교체나
# 레이턴시 튜닝이 필요할 때만 해당 env (OLLAMA_MODEL, OLLAMA_CHAT_MODEL, OLLAMA_KEEP_ALIVE,
# EDGE_TTS_VOICE, EDGE_TTS_PITCH_PCT, EDGE_TTS_RATE 등) 를 여기 추가해 override 한다.
